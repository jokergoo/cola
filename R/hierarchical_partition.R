HierarchicalPartition = setClass("HierarchicalPartition",
    slots = list(
        part_list = "list",
        hirerachy = ""
    )
)

hierarchical_partition = function(data, ...) {

    part = consensus_partition(data, ...)
}

most_tight_subgroup = function() {

}

